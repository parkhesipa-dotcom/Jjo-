validated_response = PANResponse(**result)

            # ✅ Optional local business rule:
            # If the API indicates missing consent or any failure status, return 400
            if (
                validated_response.statusCode != 101
                or "Consent Missing" in str(result).title()
            ):
                return validated_response.model_dump(), 400

            # Otherwise, return success
            return validated_response.model_dump(), 200
